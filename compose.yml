services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    environment:
      ANEMOCHORE_API_URL: ${ANEMOCHORE_API_URL}
      ANEMOCHORE_API_KEY: ${ANEMOCHORE_API_KEY}
      ANEMOCHORE_PUBLIC_ORIGIN: ${ANEMOCHORE_PUBLIC_ORIGIN}
      XYLEM_DELETE_SECRET: ${XYLEM_DELETE_SECRET}
      XYLEM_STATIC_DIR: ${XYLEM_STATIC_DIR}
      BINDING: ${BINDING}
      PORT: ${PORT}
    command: npm run dev

volumes:
  node_modules:
